void mousePressed() {
  if (mode == start) {
    
  } else if (mode == playing) {
    
  } else if (mode == paused) {
    
  } else if (mode == over) {
     
  }
}

void mouseReleased() {
  if (mode == start) {
    start1.click();
  } else if (mode == playing) {
    
  } else if (mode == paused) {
    
  } else if (mode == over) {
     
  }
}
